
# adbot user

```bash
# adbot user
NAME:
   adbot user - user management

USAGE:
   adbot user command [command options] [arguments...]

COMMANDS:
     any          has any users
     ls           list all of users
     profile      show current user's profiles
     session      list current user's sessions
     create       create a user
     chgpassword  change user password
```
